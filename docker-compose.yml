services:
  server:
    image: oven/bun:1
    container_name: server
    working_dir: /app
    volumes:
      - ./apps/server:/app
      - server_node_modules:/app/node_modules
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    env_file:
      - ./apps/server/.env
    command: sh -c "bun install && bunx nodemon --legacy-watch --exec bun run server.js"
    tty: true
    stdin_open: true

  client:
    image: oven/bun:1
    container_name: client
    working_dir: /app
    volumes:
      - ./apps/client:/app
      - client_node_modules:/app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - VITE_PROXY_TARGET=http://server:8000
    env_file:
      - ./apps/client/.env
    command: sh -c "bun install && bun run dev -- --host 0.0.0.0"
    depends_on:
      - server
    tty: true
    stdin_open: true

  superadmin:
    image: oven/bun:1
    container_name: superadmin
    working_dir: /app
    volumes:
      - ./apps/superadmin:/app
      - superadmin_node_modules:/app/node_modules
    ports:
      - "5174:5174"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - VITE_PROXY_TARGET=http://server:8000
    command: sh -c "bun install && bun run dev -- --host 0.0.0.0 --port 5174"
    depends_on:
      - server
    tty: true
    stdin_open: true

volumes:
  server_node_modules:
  client_node_modules:
  superadmin_node_modules:
